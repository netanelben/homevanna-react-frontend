import React from "react";
import "./style.scss";

const UploadLetter = () => (
  <div className="UploadLetter">
    <div className="sub-title">Upload a Pre-approval Letter</div>

    <p className="content">
      A pre-approval letter could make your offer be perceived as stronger
      compared to an offer without one, and it could also help expedite the
      closing process.
    </p>

    <a href="#">Login to upload</a>
  </div>
);

export default UploadLetter;
